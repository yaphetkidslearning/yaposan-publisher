# Worker and Queue Health

During burn-in, inspect worker heartbeats, active leases, retries, failed jobs, queue age, renderer timeouts, R2 upload results, and temporary-file cleanup.

Rollback or pause new exports when jobs are repeatedly reclaimed, queue age exceeds the operating threshold, artifacts are missing, or multiple workers complete the same job.
