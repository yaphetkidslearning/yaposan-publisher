export type ModelPrice={inputUsdPerMillion:number;outputUsdPerMillion:number};
function defaults():ModelPrice{return {inputUsdPerMillion:Math.max(0,Number(process.env.AI_DEFAULT_INPUT_USD_PER_MILLION_TOKENS??'1')),outputUsdPerMillion:Math.max(0,Number(process.env.AI_DEFAULT_OUTPUT_USD_PER_MILLION_TOKENS??'4'))}}
function catalog():Record<string,ModelPrice>{try{const raw=JSON.parse(process.env.AI_MODEL_PRICING_JSON??'{}');return raw&&typeof raw==='object'?raw:{}}catch{return {}}}
export function modelPrice(provider?:string,model?:string):ModelPrice{const c=catalog();const keys=[provider&&model?`${provider.toLowerCase()}:${model}`:'',model??'',provider?.toLowerCase()??''].filter(Boolean);for(const key of keys){const x=(c as any)[key];if(x&&Number.isFinite(Number(x.inputUsdPerMillion))&&Number.isFinite(Number(x.outputUsdPerMillion)))return {inputUsdPerMillion:Math.max(0,Number(x.inputUsdPerMillion)),outputUsdPerMillion:Math.max(0,Number(x.outputUsdPerMillion))};}return defaults()}
export function providerCostMicros(inputTokens:number,outputTokens:number,provider?:string,model?:string){const p=modelPrice(provider,model);return Math.max(1,Math.ceil(inputTokens*p.inputUsdPerMillion+outputTokens*p.outputUsdPerMillion))}
