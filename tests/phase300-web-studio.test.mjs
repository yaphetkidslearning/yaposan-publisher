import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
const read=p=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('Phase 30 web studio and engine are integrated',()=>{const app=read('src/app/web-studio.tsx');const engine=read('src/utils/webStudioEngine.ts');assert.match(app,/Web & Interactive Studio/);assert.match(app,/desktop/);assert.match(app,/tablet/);assert.match(app,/mobile/);assert.match(app,/Prepare/);assert.match(engine,/WEB_BLOCK_LIBRARY/);assert.match(engine,/buildWebExport/)});
test('Creative suite missing workspace routes are present in home navigation',()=>{const home=read('src/app/index.tsx');for(const name of ['Web Studio','Presentation Studio','PDF & Document Tools','3D & Mockup Studio','Marketing Center'])assert.match(home,new RegExp(name.replace('&','\\&')));for(const file of ['presentation-studio.tsx','document-tools.tsx','mockup-studio.tsx','marketing-center.tsx'])assert.ok(fs.existsSync(new URL(`../src/app/${file}`,import.meta.url)))});
