import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('Phase 26 motion studio is integrated',()=>{for(const f of ['src/utils/motionStudioEngine.ts','src/components/studios/MotionWorkspace.tsx','src/app/video-studio.tsx','src/app/animation-studio.tsx'])assert.equal(fs.existsSync(f),true);const x=fs.readFileSync('src/components/studios/MotionWorkspace.tsx','utf8');for(const term of ['Multi-track timeline','Keyframes & motion paths','Split','Social resize & export'])assert.match(x,new RegExp(term.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));});
