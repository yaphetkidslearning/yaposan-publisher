import test from 'node:test';
import assert from 'node:assert/strict';
import fs from 'node:fs';
const read=p=>fs.readFileSync(p,'utf8');
test('91.18 has first-time onboarding with persistence and skip/start controls',()=>{const s=read('src/app/index.tsx');for(const x of ['FIRST-TIME GUIDE','yaposan.guidance.onboarding91.18.dismissed','Skip','Start creating','Open full How Yaposan Works guide'])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))});
test('Help search covers guides and FAQ and includes shortcuts plus glossary',()=>{const s=read('src/app/help.tsx');for(const x of ['results.faqs','Keyboard shortcuts','Yaposan glossary','Your first 5 minutes','/faq','/troubleshoot'])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))});
test('FAQ is dedicated searchable categorized Q&A',()=>{const s=read('src/app/faq.tsx');for(const x of ['FAQ & Q/A','Search questions','categories','helpFaqs','/troubleshoot','/contact'])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.ok((read('src/utils/helpCenterContent.ts').match(/question:/g)||[]).length>=40)});
test('Troubleshooting includes live provider readiness and six diagnostic flows',()=>{const s=read('src/app/troubleshoot.tsx');for(const x of ['Service readiness','/api/v1/ai/media/capabilities','AI generation','Remove Background','Saving / Projects','Export','Sign in / Account','Website / Publishing'])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')))});
test('shared workspace shell exposes contextual Help',()=>{const s=read('src/components/workspace/Phase241IShell.tsx');assert.match(s,/help\?topic=/);assert.match(s,/Help/)});
test('Contact collects production support details without faking delivery',()=>{const s=read('src/app/contact.tsx');for(const x of ['Your name','Email','Category','Priority','Attach screenshot or file','Include non-secret diagnostics','Delivery is not connected yet','91.18'])assert.match(s,new RegExp(x.replace(/[.*+?^${}()|[\]\\]/g,'\\$&')));assert.doesNotMatch(s,/ticket has been sent|request was submitted/i)});
test('Photo Studio and project empty states point to help',()=>{assert.match(read('src/app/photo-studio.tsx'),/Troubleshoot image tools/);const p=read('src/app/projects.tsx');assert.match(p,/How Projects Work/);assert.match(p,/Create your first project/)});
test('public support routes are SEO-visible and auth routes remain absent',()=>{const s=read('public/sitemap.xml');for(const u of ['/help','/faq','/troubleshoot','/contact'])assert.match(s,new RegExp(u));assert.doesNotMatch(s,/\/sign-in|\/register/);const sep=read('src/utils/siteSeparation.ts');assert.match(sep,/\/faq/);assert.match(sep,/\/troubleshoot/)});
