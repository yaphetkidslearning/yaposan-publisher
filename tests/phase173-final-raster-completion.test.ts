import test from "node:test"; import assert from "node:assert/strict";
import { addRasterExportRecipe, buildRasterDeliveryManifest, buildRasterRenderPlan, setFrequencySeparation, setRasterCompositeStack, setRasterHDR, setRasterLut, setRasterMetadata, setRasterRawDevelopment, validateRasterRenderPlan } from "../src/utils/professionalRasterEngine";
const base:any={id:"img",type:"image",imageUri:"file:///photo.raw",width:1200,height:800};
test("Phase 17.3 RAW, HDR and frequency separation normalize",()=>{let e:any=setRasterRawDevelopment(base,{enabled:true,format:"raw",denoise:150});e=setRasterHDR(e,{enabled:true,sourceCount:3,strength:150});e=setFrequencySeparation(e,{enabled:true,radius:500});assert.equal(e.rasterRawDevelopment.denoise,100);assert.equal(e.rasterHDR.strength,100);assert.equal(e.rasterFrequencySeparation.radius,250)});
test("Phase 17.3 compositing LUT metadata and delivery recipes",()=>{let e:any=setRasterCompositeStack(base,{mode:"panorama",sources:["a","b"]});e=setRasterLut(e,{enabled:true,name:"Film",intensity:.8});e=setRasterMetadata(e,{rating:5,keywords:[" portrait ","portrait","studio"]});e=addRasterExportRecipe(e,{name:"Print",format:"tiff",bitDepth:16});const plan=buildRasterRenderPlan(e);const manifest=buildRasterDeliveryManifest(e);assert.equal(plan.compositeMode,"panorama");assert.equal(plan.lut,"Film");assert.equal(manifest.recipes.length,1);assert.deepEqual(e.rasterMetadata.keywords,["portrait","studio"])});
test("Phase 17.3 audit detects incomplete HDR and stacks",()=>{let e:any=setRasterHDR(base,{enabled:true,sourceCount:1});e=setRasterCompositeStack(e,{mode:"focus-stack",sources:["a"]});assert.ok(validateRasterRenderPlan(e).length>=2)});
