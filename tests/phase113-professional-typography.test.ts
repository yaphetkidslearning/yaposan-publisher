import test from "node:test";import assert from "node:assert/strict";import {applyFontPairing,applyTypographyStyle,normalizeProfessionalTypography,replaceTypography,saveTypographyStyle,typographyIssues} from "../src/utils/professionalTypography";import type {PublisherElement,PublisherProject} from "../src/types/publisher";
const text:PublisherElement={id:"t",name:"Text",type:"text",x:0,y:0,width:300,height:100,rotation:0,zIndex:1,opacity:1,text:"Hello",fontFamily:"Arial",fontSize:32};
const project:PublisherProject={id:"p",name:"P",createdAt:1,updatedAt:1,pages:[{id:"pg",name:"Page 1",width:800,height:1000,orientation:"portrait",sizeKey:"letter",backgroundColor:"#fff",margin:0,bleed:0,elements:[text,{...text,id:"b",fontSize:12}]}],activePageId:"pg",autoSave:true,version:2};
test("normalizes optical alignment and wrap",()=>{const n=normalizeProfessionalTypography({...text,textWrapPadding:999});assert.equal(n.textWrapMode,"none");assert.equal(n.textWrapPadding,100)});
test("find replace formatting updates matching text",()=>{const r=replaceTypography(project,{fontFamily:"Arial"},{fontFamily:"Inter"});assert.equal(r.count,2);assert.equal(r.project.pages[0].elements[0].fontFamily,"Inter")});
test("styles can be saved and applied",()=>{const p=saveTypographyStyle(project,"s","Title",{fontWeight:"900"});const e=applyTypographyStyle(text,p,"s");assert.equal(e.fontWeight,"900");assert.equal(e.professionalStyleId,"s")});
test("pairing assigns heading and body fonts",()=>{const p=applyFontPairing(project,"modern");assert.equal(p.pages[0].elements[0].fontFamily,"Montserrat");assert.equal(p.pages[0].elements[1].fontFamily,"Merriweather")});
test("preflight reports tiny text",()=>{const p={...project,pages:[{...project.pages[0],elements:[{...text,fontSize:5}]}]};assert.ok(typographyIssues(p).some(x=>x.includes("smaller than 6")))});
