import test from "node:test";import assert from "node:assert/strict";
import {assembleEditableProject,buildCreationPlan,buildProviderPlan,detectCreationIntent} from "../src/services/creativeCreationEngine";
test("91.9 routes common creation prompts",()=>{assert.equal(detectCreationIntent("Create a website for my restaurant").kind,"website");assert.equal(detectCreationIntent("make a 30 second video ad").kind,"video");assert.equal(detectCreationIntent("create a ten slide presentation").kind,"presentation");assert.equal(detectCreationIntent("design a summer flyer").destination,"Publisher")});
test("91.9 creates a plan and editable project",()=>{const plan=buildCreationPlan("Create a website for an Ethiopian restaurant");assert.ok(plan.steps.length>=4);assert.ok(buildProviderPlan(plan).some(x=>x.capability==="web"));const project=assembleEditableProject(plan);assert.equal(project.editable,true);assert.equal(project.projectType,"website");assert.equal(project.destination,"Web Studio")});
