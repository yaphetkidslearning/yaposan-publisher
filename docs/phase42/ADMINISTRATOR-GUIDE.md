# Administrator Guide

Configure organizations, workspaces, roles, seats, feature flags, retention rules and approved integrations. Require MFA for privileged roles. Review audit logs, active devices, API keys and billing usage. Configure backup retention and perform scheduled restore exercises. Production release requires verified health checks, signed artifacts, approved legal documents and completed security/accessibility reports.
