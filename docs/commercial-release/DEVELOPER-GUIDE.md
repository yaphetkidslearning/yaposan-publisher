# Developer Guide

Run `npm ci`, `npm run typecheck`, and `npm run verify:phase24.0dh`. Desktop builds use Electron Builder. Commercial release services are exposed through `commercialReleaseCompletionEngine.ts`; UI integration is in `CommercialReleaseCompletionModal.tsx`.
