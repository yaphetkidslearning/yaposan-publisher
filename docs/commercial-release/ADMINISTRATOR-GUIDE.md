# Administrator Guide

Administrators manage deployment channels, update URLs, code signing, telemetry policy, license issuance endpoints, device limits, and release validation. Keep signing keys and license issuer secrets outside the client application. Require HTTPS for update and activation services.
