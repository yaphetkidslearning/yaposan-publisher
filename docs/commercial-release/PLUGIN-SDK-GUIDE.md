# Plugin SDK Guide

Plugins must declare an identifier, version, compatibility range, permissions, commands, and cleanup hooks. Plugins may not access files, network resources, or project content without explicit permission. Validate plugin manifests before activation.
