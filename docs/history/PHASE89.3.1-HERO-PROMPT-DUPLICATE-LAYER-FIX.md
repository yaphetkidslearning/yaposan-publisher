# Phase 89.3.1 - Hero Prompt Duplicate Layer Fix

- Added a dark artwork-matched masking layer over the prompt bar baked into the Phase 89.1 hero image.
- Kept the real editable TextInput and Generate button above the mask.
- Prevented the old decorative prompt and the interactive prompt from appearing simultaneously.
- Preserved compact bottom-right placement and mobile behavior.
