# Yaposan Publisher v1.0.0 RC9

RC9 adds cryptographically signed backup manifests, artifact checksum validation, required PostgreSQL/object-storage/configuration coverage, exact restore confirmation, production restore boundaries, retention checks, backup freshness checks, restore-drill checks, and cross-region-copy readiness.

Local validation proves the safety logic. Real PostgreSQL dumps, R2 replication, secret-manager exports, scheduled backups, and an isolated restore drill still require deployed infrastructure.
