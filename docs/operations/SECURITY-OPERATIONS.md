# Security Operations

Require HTTPS, MFA for privileged accounts, least privilege, protected metrics, restricted CORS, trusted-proxy configuration only behind the known proxy, rate limits, request bounds, signed webhooks, signed licenses and backups, isolated renderer execution, audit logs, secret rotation, dependency scanning, and incident evidence preservation. Treat unexpected administrator, billing, storage, or authentication changes as security events.
